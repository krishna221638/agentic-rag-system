# Agentic RAG System

Agent that answers queries using:

- Structured data (CSV)
- Unstructured data (PDF)
- Web search

## Tools:

- `query_data`: For querying structured financial data.
- `search_docs`: For searching through unstructured PDF documents.
- `web_search`: For performing real-time web searches.
